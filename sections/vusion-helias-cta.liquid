{{ 'vusion-helias.css' | asset_url | stylesheet_tag }}

{%- liquid
  if section.settings.button_1_link != blank
    assign button_1_link = section.settings.button_1_link
  else
    assign button_1_link = '#experience'
  endif
  if section.settings.button_2_link != blank
    assign button_2_link = section.settings.button_2_link
  else
    assign button_2_link = '#models'
  endif
-%}

<script src="{{ 'vusion-helias.js' | asset_url }}" defer="defer"></script>

<div class="vusion-helias vusion-helias-final vusion-helias-band">
  <div class="vusion-helias-wrap vusion-helias-reveal">
    {%- if section.settings.eyebrow != blank -%}
      <div class="vusion-helias-eyebrow vusion-helias-final__eyebrow">{{ section.settings.eyebrow | escape }}</div>
    {%- endif -%}
    {%- if section.settings.heading != blank -%}
      <h2 class="vusion-helias-disp">{{ section.settings.heading | escape }}</h2>
    {%- endif -%}
    {%- if section.settings.lede != blank -%}
      <p>{{ section.settings.lede | escape }}</p>
    {%- endif -%}
    <div class="vusion-helias-btn-row">
      {%- if section.settings.button_1_label != blank -%}
        <a class="vusion-helias-btn vusion-helias-btn--primary" href="{{ button_1_link }}">
          {{ section.settings.button_1_label | escape }}
        </a>
      {%- endif -%}
      {%- if section.settings.button_2_label != blank -%}
        <a class="vusion-helias-btn vusion-helias-btn--outline" href="{{ button_2_link }}">
          {{ section.settings.button_2_label | escape }}
        </a>
      {%- endif -%}
    </div>
  </div>
</div>

{% stylesheet %}
  .vusion-helias.vusion-helias-final {
    text-align: center;
    background:
      radial-gradient(ellipse 800px 400px at 50% 0%, rgba(225, 35, 26, 0.14), transparent 65%),
      var(--vh-bg);
    border-top: 1px solid var(--vh-hairline);
  }

  .vusion-helias-final__eyebrow {
    justify-content: center;
  }

  .vusion-helias-final h2 {
    font-size: clamp(var(--vh-fs-h3), 3.2vw, var(--vh-fs-h2));
    margin: 0 0 22px;
  }

  .vusion-helias-final p {
    color: var(--vh-ink-dim);
    font-size: var(--vh-fs-lede);
    margin: 0 0 40px;
  }

  .vusion-helias-final .vusion-helias-btn-row {
    justify-content: center;
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Vusion Helias CTA",
  "tag": "section",
  "class": "vusion-helias-section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Ready when you are"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Ready to experience Helias?"
    },
    {
      "type": "textarea",
      "id": "lede",
      "label": "Lede",
      "default": "Test it on your own ears, in your own booth, before you decide."
    },
    {
      "type": "text",
      "id": "button_1_label",
      "label": "Primary button label",
      "default": "Book Demo"
    },
    {
      "type": "url",
      "id": "button_1_link",
      "label": "Primary button link",
      "info": "Empty uses #experience"
    },
    {
      "type": "text",
      "id": "button_2_label",
      "label": "Secondary button label",
      "default": "Buy Now"
    },
    {
      "type": "url",
      "id": "button_2_link",
      "label": "Secondary button link",
      "info": "Empty uses #models"
    }
  ],
  "presets": [
    {
      "name": "Vusion Helias CTA"
    }
  ]
}
{% endschema %}
